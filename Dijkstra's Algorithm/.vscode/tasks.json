{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Compile and Execute",
            "type": "shell",
            "command": "g++ main.cpp -g -o result;./result;",
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}